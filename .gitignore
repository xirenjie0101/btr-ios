node_modules/
# generated by test/setup.sh (colour-bar streams, throw-away TLS certificate) and by the test runs
test/media/
test/certs/
test/artifacts/
# build by-product that only the tests use; everything Loon loads is committed
dist/player.html
